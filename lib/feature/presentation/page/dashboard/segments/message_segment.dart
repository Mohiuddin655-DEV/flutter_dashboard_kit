import 'package:flutter/material.dart';

class MessageSegment extends StatelessWidget {
  const MessageSegment({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      width: 150,
      color: Colors.blue.shade100,
    );
  }
}
